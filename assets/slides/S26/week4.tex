% Copyright 2026  Ed Bueler

\documentclass[10pt,
               svgnames,
               hyperref={colorlinks,
                         citecolor=DeepPink4,
                         linkcolor=FireBrick,
                         urlcolor=blue},
               usepdftitle=false]{beamer}

\usetheme{Madrid}
\usecolortheme{beaver}
\setbeamercovered{transparent}
\setbeamerfont{frametitle}{size=\large}
\setbeamercolor*{block title}{bg=red!10}
\setbeamercolor*{block body}{bg=red!5}

\usepackage[english]{babel}
\usepackage[latin1]{inputenc}
\usepackage{times}
\usepackage[T1]{fontenc}
% Or whatever. Note that the encoding and the font should match. If T1
% does not look nice, try deleting the line with the fontenc.

\usepackage{fancyvrb,empheq,bm,xspace,dsfont}
%\usepackage{minted}
\usepackage{tikz}

% If you wish to uncover everything in a step-wise fashion, uncomment
% the following command: 
%\beamerdefaultoverlayspecification{<+->}

\newcommand{\bb}{\mathbf{b}}
\newcommand{\bc}{\mathbf{c}}
\newcommand{\bbf}{\mathbf{f}}
\newcommand{\bl}{\bm{\ell}}
\newcommand{\br}{\mathbf{r}}
\newcommand{\bs}{\mathbf{s}}
\newcommand{\bx}{\mathbf{x}}
\newcommand{\by}{\mathbf{y}}
\newcommand{\bv}{\mathbf{v}}
\newcommand{\bu}{\mathbf{u}}
\newcommand{\bw}{\mathbf{w}}

\newcommand{\bzero}{\bm{0}}

\newcommand{\CC}{\mathbb{C}}
\newcommand{\NN}{\mathbb{N}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\ZZ}{\mathbb{Z}}

\newcommand{\one}{\mathds{1}}

\newcommand{\range}{\operatorname{range}}
\newcommand{\Span}{\operatorname{span}}

\newcommand{\Matlab}{\textsc{Matlab}\xspace}
\newcommand{\eps}{\epsilon}

\newcommand{\ds}{\displaystyle}

\newcommand{\ip}[2]{\left<#1,#2\right>}

\newcommand{\twovect}[4]{\ensuremath{{#1}_{#2} =
                            \begin{bmatrix} #3 \\ #4 \end{bmatrix}}}

\newcommand{\rbullet}{{\color{FireBrick} \bullet}}

\newcommand*\circled[1]{\tikz[baseline=(char.base)]{
            \node[shape=circle,draw,inner sep=2pt] (char) {#1};}}

\newcommand{\textbook}{Saxe, \emph{Beginning Functional Analysis}}

\DefineVerbatimEnvironment{mVerb}{Verbatim}{numbersep=2mm,
frame=lines,framerule=0.1mm,framesep=2mm,xleftmargin=4mm,fontsize=\scriptsize}

\newcommand{\itemnum}[1]{\setcounter{enumi}{#1}\usebeamertemplate{enumerate item}}



\title{Fourier series of continuous functions}

\subtitle{a calculation for weeks 4 \& 5 \quad (\emph{version 2})}

\author{Ed Bueler}

\institute[]{UAF Math 617 Functional Analysis}

\date{Spring 2026}


\begin{document}
\beamertemplatenavigationsymbolsempty

\begin{frame}
  \maketitle
\end{frame}


\begin{frame}{Outline}
  \tableofcontents[hideallsubsections]
\end{frame}

\AtBeginSection[]
{% do not put toc here this time
}


\section{sines (and complex exponentials) are orthonormal}

\begin{frame}{the product-of-sines integral}

\begin{itemize}
\item for $n\in\NN$, $\sin(nx)$ is a \emph{wave} on $[0,\pi]$
\item suppose $m,n\in\NN$ and integrate the product of sines:
\end{itemize}
\begin{align*}
\int_0^\pi \sin(mx)\sin(nx)\,dx &= \hspace{50mm} \\
&\phantom{foo} \\
&\phantom{foo} \\
&\phantom{foo} \\
&\phantom{foo} \\
&\phantom{foo} \\
&\phantom{foo} \\
  &\hspace{50mm} = \begin{cases} \frac{\pi}{2}, & m=n \\ 0, & \text{otherwise} \end{cases}
\end{align*}

\noindent \hrulefill

\noindent {\small \alert{$\cos(\theta \pm \psi) = \cos \theta \cos \psi \mp \sin \theta \sin \psi$} \, $\therefore$ \, $\sin \theta \sin \psi = \frac{1}{2} \left(\cos(\theta-\psi) - \cos(\theta + \psi)\right)$}
\end{frame}


\begin{frame}{an infinite orthonormal set in $C[0,\pi]$}

\begin{itemize}
\item fact from last slide: \quad $\ds \int_0^\pi \sin(mx)\sin(nx)\,dx = \frac{\pi}{2} \delta_{mn}$
\item define
    $$\phi_n(x) = \sqrt{\frac{2}{\pi}} \sin(n x) \qquad \text{and} \qquad S = \left\{\phi_n(x)\,:\,n\in \NN\right\}$$
\item then:
    \begin{enumerate}
    \item $S$ is orthonormal in the (real) inner product $\ip{f}{g} = \int_0^{\pi} f(x)g(x)\,dx$:

\vspace{15mm}
    \item $S$ is linearly independent:

\vspace{15mm}
    \end{enumerate}
\item \emph{questions:}  what is $\Span(S)$?  what is $\overline{\Span(S)}$?
\end{itemize}
\end{frame}


\begin{frame}{an infinite orthonormal set in $C[0,\pi]$}

\begin{center}
\includegraphics[width=0.8\textwidth]{figs/fourier1sines.png}
\end{center}
\end{frame}


\begin{frame}{a complex-valued orthonormal set in $C[-\pi,\pi]$}

\begin{itemize}
\item \emph{easier} integral:
	$$\int_{-\pi}^\pi e^{imx} e^{-inx}\,dx = \int_{-\pi}^\pi e^{i(m-n)x}\,dx = \hspace{40mm} = 2\pi \delta_{mn}$$
\item define
    $$\psi_n(x) = \frac{1}{\sqrt{2\pi}} e^{inx} \qquad \text{and} \qquad E = \left\{\psi_n(x)\,:\,n\in \ZZ\right\}$$
\item recall \emph{complex} inner product (\emph{sesquilinear} \, :-) \,), now on $C[-\pi,\pi]$:
	$$\ip{f}{g} = \int_{-\pi}^\pi f(x) \overline{g(x)}\,dx$$
\item then:
    \begin{enumerate}
    \item $E$ is orthonormal
    \item $E$ is linearly-independent
    \end{enumerate}
\item same big \emph{question:}  \quad what is $\overline{\Span(E)}$?
\end{itemize}
\end{frame}


\begin{frame}{before we move on \dots}


\begin{block}{basic facts about $e^{i\theta}$:}
\begin{itemize}
\item $\ds e^{i\theta}=\cos\theta + i \sin\theta$
    \begin{itemize}
    \item[$\circ$] derive this from $e^z = 1 + z + \frac{z^2}{2!} + \dots + \frac{n^2}{n!} + \dots$
    \item[$\circ$] also recall Taylor series for $\cos$ and $\sin$
    \end{itemize}
\item $\ds \cos\theta = \frac{1}{2}\left(e^{i\theta}+e^{-i\theta}\right)$, \, $\ds \sin\theta = \frac{1}{2i}\left(e^{i\theta}-e^{-i\theta}\right)$
\end{itemize}
\end{block}

\medskip

\begin{block}{basic facts about even and odd Fourier series:}
\begin{itemize}
\item if $f$ is even then
    $$\int_{-\pi}^\pi f(x) e^{-inx}\,dx=\int_{-\pi}^\pi f(x) \cos(nx)\,dx=2\int_0^\pi f(x) \cos(nx)\,dx$$
\item if $f$ is odd then
    $$\int_{-\pi}^\pi f(x) e^{-inx}\,dx=i\int_{-\pi}^\pi f(x) \sin(nx)\,dx=2i\int_0^\pi f(x) \sin(nx)\,dx$$
\end{itemize}
\end{block}
\end{frame}


% at start of sections 2,3,... put outline
\AtBeginSection[] {
    \begin{frame}{Outline}
    \setbeamertemplate{section in toc}[sections numbered]
    \tableofcontents[currentsection]%,hideallsubsections]
    \end{frame}
}


\section{what did Fourier believe? (1822)}

\begin{frame}{Fourier's assertion}

\begin{block}{claim (Fourier, \emph{Th\'eorie Analytique de la Chaleur}, 1822)}
if $f\in C[0,\pi]$, and if we compute these coefficients
	$$c_n = \ip{f}{\phi_n} = \sqrt{\frac{2}{\pi}} \int_0^\pi f(x) \sin(nx)\,dx$$
for $n\in \NN$, then
	$$f(x) = \sum_{n=1}^\infty c_n\, \phi_n(x) = \sqrt{\frac{2}{\pi}} \sum_{n=1}^\infty c_n \sin(nx)$$
\end{block}

\begin{itemize}
\item the result is the \emph{Fourier sine series} (or \emph{expansion}) of $f$
\item actually Fourier claimed this for discontinuous functions too
   \begin{itemize}
   \item[$\circ$] observe that $c_n$ is an \emph{integral}, so only features of $f$ that change the integrals can affect the expansion
   \item[$\circ$] Fourier, and everybody else, agreed that the claim could not be exactly true at the points of discontinuity of $f(x)$
   \end{itemize}
\end{itemize}
\end{frame}


\begin{frame}{complex version is easier}

\begin{itemize}
\item Fourier used real numbers (to my knowledge)
\item we may replace
	$$C[0,\pi] \to C[-\pi,\pi] \qquad \text{and} \qquad \sin(nx) \to e^{inx},$$
and use the complex (sesquilinear) inner product
\end{itemize}

\begin{block}{Fourier's claim for complex exponentials}
if $f\in C[-\pi,\pi]$, complex-valued, and if
	$$c_n = \ip{f}{\psi_n} = \frac{1}{\sqrt{2\pi}} \int_{-\pi}^\pi f(x) e^{-inx}\,dx$$
for $n\in\ZZ$, then
	$$f(x) = \sum_{n\in\ZZ} c_n\, \psi_n(x) = \frac{1}{\sqrt{2\pi}} \sum_{n\in\ZZ} c_n e^{inx}$$
\end{block}

\begin{itemize}
\item this is the \emph{Fourier series} of $f$, or the \emph{complex Fourier series}
\item for formulas from different references, be careful where the $2\pi$ goes
\end{itemize}
\end{frame}


\begin{frame}{example 1: $f(x)=x$}

\begin{itemize}
\item consider the Fourier sine series for $f(x)=x$, using the real orthonormal set $\{\phi_n(x) = \sqrt{\frac{2}{\pi}} \sin(n x)\} \subset C[0,\pi]$
\item integrate to get coefficients:
\begin{align*}
c_n &= \ip{f}{\phi_n} = \sqrt{\frac{2}{\pi}} \int_0^\pi x \sin(n x)\,dx = \hspace{60mm} \\
&\phantom{x} \\
&\phantom{x} \\
&\phantom{x} \\
&\phantom{x} \\
&\phantom{x} \hspace{80mm} = \frac{\sqrt{2\pi} (-1)^{n-1}}{n}
\end{align*}
\end{itemize}
\end{frame}


\begin{frame}[fragile]
\frametitle{example 1: $f(x)=x$}

\begin{itemize}
\item \Matlab:
\begin{mVerb}
f = @(x) x;
x = 0:pi/300:pi;  N = 150;  nn = 1:N;
c = sqrt(2 * pi) * (-1).^(nn - 1) ./ nn;
sN = zeros(size(x));
for n=1:N
    sN = sN + c(n) * sqrt(2/pi) * sin(n*x);  end
plot(x, f(x), x, sN)
\end{mVerb}
\item result (works \dots but \emph{Gibbs effect}!):

\begin{center}
\includegraphics[width=0.45\textwidth]{figs/fourier1result.png}
\end{center}
\end{itemize}
\end{frame}


\begin{frame}{example 2: $g(x)$ cubic}

\begin{itemize}
\item note that if $f(x)=x$ is \emph{extended periodically} to the real line then the result is \emph{not} continuous
\end{itemize}

\vspace{-2mm}

\noindent \hrulefill
\begin{itemize}
\item instead consider $g(x) = x (x-1) (x-\pi)$ on the interval $[0,\pi]$
\item now the periodic extension \emph{is} continuous
\item coefficients, done numerically: \quad $\ds c_n = \ip{g}{\phi_n} = \sqrt{\frac{2}{\pi}} \int_0^\pi g(x) \sin(n x)\,dx$
\end{itemize}

\medskip
\begin{columns}
\begin{column}{0.5\textwidth}
\Matlab result:

\begin{center}
\includegraphics[width=0.8\textwidth]{figs/fourier2result.png}
\end{center}
\end{column}
\begin{column}{0.5\textwidth}
coefficient decay:

\begin{center}
\includegraphics[width=0.9\textwidth]{figs/coeffs12result.pdf}
\end{center}
\end{column}
\end{columns}
\end{frame}


\section{what did Dirichlet prove? (1829)}

\begin{frame}{approximating $f$}

\begin{itemize}
\item again consider complex Fourier series for $f\in C[-\pi,\pi]$, namely
   $$c_n = \frac{1}{\sqrt{2\pi}} \int_{-\pi}^\pi f(x) e^{-inx}\,dx, \qquad f(x) = \frac{1}{\sqrt{2\pi}} \sum_{n\in\ZZ} c_n e^{inx}$$
\item combine into one formula and exchange limit processes:
\begin{align*}
f(x) &= \frac{1}{\sqrt{2\pi}} \sum_{n\in\ZZ} \left(\frac{1}{\sqrt{2\pi}} \int_{-\pi}^\pi f(y) e^{-iny}\,dy\right) e^{inx} \\
&= \int_{-\pi}^\pi \left(\frac{1}{2\pi} \sum_{n\in\ZZ} e^{-iny} e^{inx} \right) f(y)\,dy = \int_{-\pi}^\pi \left(\alert{\frac{1}{2\pi} \sum_{n\in\ZZ} e^{in(x-y)}}\right) f(y)\,dy
\end{align*}

    \begin{itemize}
    \item[$\circ$] exchange of integral and sum is justified if $\sum |c_n|<\infty$
    \end{itemize}

\medskip
\item if the \alert{inner sum} is approximated by a partial sum, then we are approximating $f$ by integrating it against a kernel
\end{itemize}
\end{frame}


\begin{frame}{Dirichlet's kernel}

\begin{definition}[Dirichlet's kernel, 1829]
$$D_m(x) = \frac{1}{2\pi} \sum_{n=-m}^m e^{inx} \quad \stackrel{\text{\alert{why?}}}{=}\, \frac{1}{2\pi} + \frac{1}{\pi} \sum_{n=1}^m \cos(nx)$$
\end{definition}

\medskip
\begin{columns}
\begin{column}{0.5\textwidth}
\includegraphics[width=\textwidth]{figs/dirkern.png}
\end{column}
\begin{column}{0.4\textwidth}
{\small
Fourier's claim has become:

\begin{align*}
f(x) &= \frac{1}{\sqrt{2\pi}} \sum_{n\in\ZZ} c_n e^{inx} \\
  &= \lim_{m\to\infty} \int_{-\pi}^\pi D_m(x-y)\, f(y)\,dy
\end{align*}
}
\end{column}
\end{columns}
\end{frame}


\begin{frame}{properties of the kernel}

{\footnotesize
\begin{definition}[Dirichlet's kernel]
$$D_m(x) = \frac{1}{2\pi} \sum_{n=-m}^m e^{inx}$$
\end{definition}
}

\begin{lemma}[properties of the kernel]
\begin{enumerate}
\item $D_m$ is continuous,
\item $\ds \int_{-\pi}^\pi D_m(x)\,dx = 1$,
\item $D_m(0) = \frac{m+1/2}{\pi}$, thus $D_m(0) \to \infty$ as $m\to\infty$, and
\item $\ds D_m(x) = \frac{1}{2\pi} \frac{\sin((m+1/2)x)}{\sin(x/2)}$ \, for $x\ne 0$.
\end{enumerate}
\end{lemma}

{\small
\noindent \emph{Proof.}  Properties \itemnum{1}, \itemnum{2}, \itemnum{3} are easy from the definition of $D_m$.  Property \itemnum{4} is on the next slide. \hfill \qed
}
\end{frame}


\begin{frame}[fragile]
\frametitle{Dirichlet's kernel as a sine ratio}

Property \itemnum{4}: \, $\ds D_m(x) = \frac{1}{2\pi} \frac{\sin((m+1/2)x)}{\sin(x/2)}$

\bigskip
{\small
\noindent \emph{Proof.}  Apply knowledge of \alert{geometric series}, then use $\sin\theta = (e^{i\theta}-e^{-i\theta})/(2i)$:
\begin{align*}
\sum_{n=-m}^m e^{inx} &= \sum_{n=-m}^m \left(e^{ix}\right)^n = e^{-imx} \sum_{n=0}^{2m} \left(e^{ix}\right)^n \\
&= e^{-imx} \frac{1 - (e^{ix})^{2m+1}}{1 - e^{ix}} = \frac{e^{-imx} - e^{i(m+1)x}}{1 - e^{ix}} \\
&= \frac{e^{-i(m+1/2)x} - e^{i(m+1/2)x}}{e^{-i(x/2)} - e^{i(x/2)}} \\
&= \frac{\sin((m+1/2)x)}{\sin(x/2)}
\end{align*}
The above calculation applies for $x\ne 0$. \hfill \qed
}
\end{frame}


\begin{frame}{Dirichlet's theorem (1829)}

\begin{theorem}
If $f\in C[-\pi,\pi]$ is periodic, and if $f'(x)$ exists at $x\in[-\pi,\pi]$, then
    $$\lim_{m\to\infty} \int_{-\pi}^\pi D_m(x-y)\, f(y)\,dy = f(x),$$
and thus Fourier's claim is true:
{\small
$$f(x) = \frac{1}{\sqrt{2\pi}} \sum_{n\in\ZZ} c_n e^{inx} \quad \text{ for }\, c_n =\frac{1}{\sqrt{2\pi}} \int_{-\pi}^\pi f(x) e^{-inx}\,dx$$
}
\end{theorem}

\begin{itemize}
\item The proof is based on the \emph{Riemann-Lebesgue lemma}.  This version only requires the Riemann integral, and Dirichlet must have known it.  I will prove it after using it.
\end{itemize}

\begin{lemma}[Riemann-Lebesgue for continuous functions]
If $g \in C[a,b]$ then $\ds \lim_{n\to\infty} \int_a^b g(x) e^{inx}\,dx = 0$.
\end{lemma}
\end{frame}


\begin{frame}{Dirichlet's theorem (1829)}

{\small
\noindent \emph{Proof.}  By the periodicity of $D_m$ and $f$, and since $D_m$ is even, use $y=x+\xi$ to show
    $$\int_{-\pi}^\pi D_m(x-y)\, f(y)\,dy = \int_{-\pi}^\pi D_m(\xi)\, f(x+\xi)\,d\xi$$
By replacing $f(x+\xi)$ with $\tilde f(\xi)$, we may assume $x=0$ and $f'(0)$ exists.  Next we calculate using the proven properties of the kernel:
\begin{align*}
\int_{-\pi}^\pi D_m(\xi)\, f(\xi)\,d\xi &= \int_{-\pi}^\pi D_m(\xi)\, \left(f(\xi) - f(0)\right)\,d\xi + \int_{-\pi}^\pi D_m(\xi)\, f(0)\,d\xi \\
  &= f(0) + \frac{1}{2\pi} \int_{-\pi}^\pi \left(f(\xi) - f(0)\right)\,\frac{\sin((m+1/2)\xi)}{\sin(\xi/2)}\,d\xi
\end{align*}
However, $f'(0)$ exists, so by L'Hopital's rule,
	$$\lim_{\xi\to 0} \frac{f(\xi) - f(0)}{\sin(\xi/2)} = \lim_{\xi\to 0} \frac{f(\xi) - f(0)}{\xi} \frac{\xi}{\sin(\xi/2)} = 2 f'(0)$$
}
\end{frame}


\begin{frame}{Dirichlet's theorem (1829)}

{\small
\noindent \emph{Proof continued.}  So now we know that this function is continuous (removable discontinuity):
	$$h(x) = \begin{cases} \frac{f(x) - f(0)}{\sin(x/2)}, & x\ne 0 \\
	                       2f'(0), & x=0 \end{cases}$$
Thus by the Riemann-Lebesgue lemma:
\begin{align*}
\lim_{m\to\infty} \int_{-\pi}^\pi D_m(\xi)\, f(\xi)\,d\xi &= f(0) + \frac{1}{2\pi} \lim_{m\to\infty}  \int_{-\pi}^\pi h(\xi) \sin((m+1/2)\xi)\,d\xi \\
&= f(0) + 0 = f(0).  \hspace{30mm} \qed
\end{align*}
}
\end{frame}


\begin{frame}{Riemann-Lebesgue lemma for continuous functions}

\begin{lemma}
If $g \in C[a,b]$ then $\ds \lim_{n\to\infty} \int_a^b g(x) e^{inx}\,dx = 0$.
\end{lemma}

{\small
\noindent \emph{Proof.}  Let $\eps>0$.  Since $g$ is continuous on the compact set $[a,b]$, it is uniformly continuous, so there is $\delta>0$ such that $|x-y|<\delta \implies |g(x)-g(y)|<\eps/(2(b-a))$.  In particular we can define a mesh of points $\{x_i\}_{i=0}^M$, with spacing less than $\delta$, and $x_i^*$ at the midpoints of each $[x_{i-1},x_i]$, so that
	$$\tilde g(x) = \sum_{i=1}^M g(x_i^*) \one_{[x_{i-1},x_i)}(x)$$
is a uniform approximation of $g$: \quad $\ds \|\tilde g - g\|_\infty = \sup_{x\in [a,b]} |\tilde g(x) - g(x)| < \frac{\eps}{2(b-a)}$

picture:

\vspace{25mm}
}
\end{frame}


\begin{frame}{Riemann-Lebesgue lemma for continuous functions}

{\small
\noindent \emph{Proof continued.}  Now
\begin{align*}
\left|\int_a^b g(x) e^{inx}\,dx\right| &\le \left|\int_a^b (g(x) - \tilde g(x)) e^{inx}\,dx\right| + \left|\int_a^b \tilde g(x) e^{inx}\,dx\right| \\
&\le \|g-\tilde g\|_\infty \int_a^b |e^{inx}|\,dx + \left|\int_a^b \sum_{i=1}^M g(x_i^*) \one_{[x_{i-1},x_i)}(x) e^{inx}\,dx\right| \\
&\le \frac{\eps}{2(b-a)}(b-a) + \sum_{i=1}^M |g(x_i^*)| \left|\int_{x_{i-1}}^{x_i} e^{inx}\,dx\right|
\end{align*}
But each of the final integrals is bounded by the same constant:
    $$\left|\int_{x_{i-1}}^{x_i} e^{inx}\,dx\right| = \left|\frac{e^{i n x_i} - e^{i n x_{i-1}}}{in}\right| \le \frac{2}{n}$$
Choose $N$ so that $(2\|g\|_\infty M)/N < \eps/2$.  If $n\ge N$ then
  $$\left|\int_a^b g(x) e^{inx}\,dx\right| \le \frac{\eps}{2} + \frac{2}{n} \sum_{i=1}^M |g(x_i^*)| \le \frac{\eps}{2} + \frac{2}{N} \sum_{i=1}^M \|g\|_\infty < \frac{\eps}{2} +  \frac{\eps}{2} = \eps \hspace{5mm} \qed$$
}
\end{frame}


\section{what is the full story for Fourier series on $C[-\pi,\pi]$? (1966)}

\begin{frame}{does the Fourier series converge for any $f\in C[-\pi,\pi]$?}

\begin{itemize}
\item in 1873 Du Bois-Reymond constructed a continuous function for which the Fourier series \alert{does not} converge at a point
    \begin{itemize}
    \item[$\circ$] \href{https://www.mathcounterexamples.net/continuous-function-with-divergent-fourier-series/}{found by google search}: \quad $\ds f(x) = \sum_{k=1}^\infty \frac{1}{k^2} \sin\left(\frac{(2^{k^3}+1)|x|}{2}\right)$
    \item[$\circ$] the function is even and continuous,\footnote{use 
Weierstrass M-test} so

its Fourier series is a cosine series
    \item[$\circ$] it can be shown that the Fourier partial

sums are unbounded in a neighborhood

of $x=0$
    \end{itemize}

\vspace{-19mm}

\hfill \includegraphics[width=0.35\textwidth]{figs/duboisreymond.png}

\medskip
\item in the early 1900s, further examples were constructed where convergence failed on infinite sets of points
\item to state the full situation we need to define \emph{measure zero} \dots
\end{itemize}
\end{frame}


\begin{frame}{sets of measure zero}

\begin{definition}
\begin{itemize}
\item a set $E\subset \RR$ has \emph{(Lebesgue) measure zero} if for any $\eps>0$ there exists a countable list of open intervals $I_n=(a_n,b_n)$ so that
    $$E \subset \bigcup_{n\in\NN} I_n \quad \text{and} \quad \sum_{n\in\NN} m(I_n) = \sum_{n\in\NN} b_n-a_n < \eps$$
\item if a proposition holds except on a set $E$ which has measure zero then we say that it holds \emph{almost everywhere}
\end{itemize}
\end{definition}

\begin{itemize}
\item note that any open subset of $\RR$ is a countable union of open intervals
\item more on Lebesgue measure $m$, and measure zero, in Chapter 3
\end{itemize}
\end{frame}


\begin{frame}{the full story for Fourier series of continuous functions}

\begin{theorem}[Carleson, 1966]
if $f\in C[-\pi,\pi]$, or even $f\in L^p[-\pi,\pi]$, then the Fourier series of $f$ converges pointwise almost everywhere:
	$$\lim_{m\to\infty} \sum_{n=-m}^m c_n e^{inx} = f(x) \quad \text{except on $x\in E$ of measure zero}$$
\end{theorem}

\begin{theorem}[Katznelson, 1966]
for any set $E\subset [-\pi,\pi]$ of measure zero there is a continuous periodic function $f\in C[-\pi,\pi]$ whose Fourier series diverges at all points of $E$
\end{theorem}
\end{frame}


\section{what is the clean $L^2$ version of the story? ($\sim$1910)}

\begin{frame}{orthonormal sequences in inner product spaces}

\begin{itemize}
\item the abstract ideas of Hilbert and Riesz, circa 1910, are fundamentally \emph{simpler} than all of the previous material in these slides
\item consider an abstract inner product vector space $V, \ip{\cdot}{\cdot}$
\end{itemize}

\begin{definition}
\begin{itemize}
\item a pair of vectors $v,w \in V$ are \emph{orthogonal} if $\ip{v}{w}=0$
\item a set $S$ is \emph{orthogonal} if $v,w\in S$ and $v\ne w$ implies $v,w$ are orthogonal
\item $(v_k)_{k=1}^\infty \subset V$ is an \emph{orthonormal sequence} (\emph{ON sequence}) if $v_k,v_l$ are orthogonal for $k\ne l$ and if $\|v_k\|^2 = \ip{v_k}{v_k} = 1$
\end{itemize}
\end{definition}

\begin{lemma}
\begin{itemize}
\item an orthogonal set is linearly-independent
\item an ON sequence is linearly-independent
\end{itemize}
\end{lemma}
\end{frame}


\begin{frame}{examples of orthonormal sequences}

{\small
\begin{itemize}
\item $\ds \left\{\sqrt{\frac{2}{\pi}} \sin(nx)\right\}_{n\in\NN}$ is a (real) ON sequence on $[0,\pi]$ \only<2>{\alert{\hfill $\longleftarrow$ complete}}
\item $\ds \left\{\frac{1}{\sqrt{2\pi}} e^{inx}\right\}_{n\in\ZZ}$ is an ON sequence on $[-\pi,\pi]$ \only<2>{\alert{\hfill $\longleftarrow$ complete}}
\item $\ds \left\{\frac{1}{\sqrt{2\pi}} e^{inx}\right\}_{n\in\NN}$ is an ON sequence on $[-\pi,\pi]$ \only<2>{\alert{\hfill $\longleftarrow$ \emph{not} complete}}
\item $\ds \left\{\sqrt{\frac{2}{\pi}} \cos(nx)\right\}_{n\in\NN}$ is a (real) ON sequence on $[0,\pi]$ \only<2>{\alert{\hfill $\longleftarrow$ \emph{not} complete}}
\item $\ds \left\{\sqrt{\frac{1}{\pi}} \sin(nx)\right\}_{n\in\NN}$ is a (real) ON sequence on $[-\pi,\pi]$ \only<2>{\alert{\hfill $\longleftarrow$ \emph{not} complete}}
\item the Legendre polynomials\footnote{with a different normalization these are:  $1,x,\frac{1}{2}(3x^2-1),\frac{1}{2}(5x^3-3x),\dots$} form an ON sequence \only<2>{\alert{\hfill $\longleftarrow$ complete}}
\end{itemize}
}
\end{frame}


\begin{frame}{fundamental question about an orthonormal sequence}

\begin{block}{question}
\begin{itemize}
\item given an ON sequence $S=(f_k)$ from an inner product space $(V,\ip{\cdot}{\cdot})$, is its span dense in $V$?:
    $$\text{is }\,\ds \overline{\Span S} = V?$$
\end{itemize}
\end{block}

\begin{itemize}
\item this question becomes a definition \dots
\end{itemize}

\begin{definition}
an ON sequence $S=(f_k)$ in an inner product space $(V,\ip{\cdot}{\cdot})$ is \emph{complete} if for every $f\in V$ there exist coefficients $c_k$ so that
	$$f = \lim_{N\to\infty} \sum_{k=1}^N c_k f_k = \sum_{k=1}^\infty c_k f_k$$
\end{definition}

\begin{itemize}
\item language: \emph{complete ON sequence} $=$ \emph{ON basis}
\end{itemize}
\end{frame}


\begin{frame}{abstract Fourier coefficients and Fourier series}

\begin{block}{Theorem 4.1}
Suppose that $f = \sum_{k=1}^\infty c_k f_k$ for an ON sequence $S=(f_k)$ in an inner product space $V,\ip{\cdot}{\cdot}$.  Then
	$$c_k = \ip{f}{f_k}$$
\end{block}

\begin{itemize}
\item the proof is easy \dots we will do it
\end{itemize}

\begin{definition}
\begin{itemize}
\item for $f\in V$, the \emph{Fourier coefficients} for an ON sequence $S=(f_k)$ are
 	$$c_k = \ip{f}{f_k}$$
\item the \emph{Fourier series of $f\in V$}, for an ON sequence $S=(f_k)$, is
	$$\sum_{k=1}^\infty \ip{f}{f_k} f_k$$
\end{itemize}
\end{definition}
\end{frame}


\begin{frame}{how to prove an ON sequence is complete?}

\begin{itemize}
\item the Fourier series is always in $V$, but that does \emph{not} imply, by itself, that it equals $f$
\item \emph{if} the ON sequence is complete then $f$ equals its Fourier series
\item part (c) of the following theorem is the most practical way to show an ON sequence of functions on an interval is complete
\end{itemize}

\begin{block}{Theorem 4.5}
for an ON sequence $S=(f_k)_{k\in\NN}$ in $L^2=L^2([-\pi,\pi],m)$, the following are equivalent:
\begin{itemize}
\item[(a)] $S$ is a complete ON sequence
\item[(b)] for every $f\in L^2$ and $\eps>0$ there is a finite linear combination $g = \sum_{k=1}^n d_k f_k$ so that $\|f-g\|_2 < \eps$
\item[(c)] if the Fourier coefficients of $f\in L^2$, for the ON sequence $S$, are zero then the function $f$ is zero almost everywhere
\end{itemize}
\end{block}

\begin{itemize}
\item we will prove this Theorem
\end{itemize}
\end{frame}


\begin{frame}{Fourier's claim is true \emph{if} convergence is in $L^2$}

\begin{block}{Theorem 4.6 (rewritten in complex form)}
The ON sequence $S = \left(\frac{1}{\sqrt{2\pi}} e^{inx}\right)_{n\in\ZZ}$ is complete in $L^2([-\pi,\pi],m)$.  Therefore
	$$f(x) = \frac{1}{\sqrt{2\pi}}\sum_{n=-\infty}^\infty c_n e^{inx},$$
for these coefficients
    $$c_n = \frac{1}{\sqrt{2\pi}} \int_{-\pi}^\pi f(x) e^{-inx}\,dx,$$
in the sense that \alert{the partial sums converge to $f$ in the norm $\|\cdot\|_2$}
\end{block}

\begin{itemize}
\item this is the simplest interpretation of Fourier's claim
    \begin{itemize}
    \item[$\circ$] however, it does address continuous functions directly
    \end{itemize}
\item we will get to $L^2$ spaces seriously in Chapter 3
\end{itemize}
\end{frame}


\section{theory from the book}

\begin{frame}{theory in weeks 4 \& 5: Chapter 4 in Saxe}

\begin{itemize}
\item we will do Fourier series (Chapter 4) \emph{before} Lebesgue integrals and measures (Chapter 3)
\end{itemize}

\begin{block}{to define:}
\begin{itemize}
\item $L^2([a,b],m)$ and $L^2(X,\mu)$ \hfill \alert{$\longleftarrow$ not fully defined till Chapter 3}
\item complete orthonormal sequence
\item Fourier series and coefficients for any orthonormal sequence
\end{itemize}
\end{block}

\begin{block}{to prove:}
\begin{itemize}
\item Bessel's inequality
\item Parseval's equality
\item the Riesz-Fischer theorem (in old and ``full-power'' forms)
\item Theorem 4.6: Fourier series converge in $L^2$
\end{itemize}
\end{block}

\begin{itemize}
\item Assignment 3 is posted at \quad  \href{https://bueler.github.io/fa/index.html}{\texttt{bueler.github.io/fa}}
\end{itemize}
\end{frame}


\end{document}
